# BL-003 requires no runtime environment values.
#
# Later bounded backlog items may activate the server-only names below. Keep
# real values in ignored local environment files and in environment-scoped
# platform settings. Never expose private values through NEXT_PUBLIC_ names.
#
# SUPABASE_URL=
# SUPABASE_PUBLISHABLE_KEY=
# MODEL_API_KEY=
# MODEL_ID=
